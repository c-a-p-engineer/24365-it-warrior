# AGENTS.md

このリポジトリで作業するAIエージェントおよび開発者向けの基本ルールです。

## 作業前に読む資料

1. `README.md`
   - プロジェクト概要、公開URL、現在の実装範囲
2. `docs/game-design.md`
   - 目grepのルール、検索条件、スコア、終了条件
3. `docs/game-loop.md`
   - ログ生成、正解・誤検知・見落とし、コンボ進行
4. `docs/ui-design.md`
   - スマートフォンUI、説明なしの導線、アニメーション
5. `docs/assets.md`
   - 仮素材、本番素材、画像・音声仕様
6. `index.html` / `styles.css` / `app.js`
   - 現在の実装

仕様変更時は、実装と関連資料を同時に更新してください。

## 固定方針

- ゲーム名は **24365 IT戦士 ― 目grep**
- GitHub Pagesで公開する
- 1人用ブラウザゲームとする
- ネットワーク対戦、ログイン、外部APIを前提にしない
- スマートフォン縦画面を優先する
- プレイ画面はページスクロールなしの1画面に収める
- 操作はログ行のタップのみで成立させる
- 説明文を読ませるチュートリアルを作らない
- 最初の正解行を発光と指マークで示す
- 通常条件は最大2条件までとする
- ログは横スクロールを必要としない長さにする
- 正解、誤検知、見落としをアニメーションと文字の両方で伝える
- 常時アニメーションを入れるが、ログの可読性を優先する
- `prefers-reduced-motion`へ対応する
- ハイスコアはスコア、最大コンボ、発見件数、正解率を保存する

## 実装上の注意

- GitHub Pagesのサブパスで動く相対パスを使用する
- サーバーサイド処理を使わない
- セーブはLocalStorageを使用する
- 320px幅で横スクロールを発生させない
- タップ領域は44px以上を基準とする
- 文字列生成と描画処理を分離できる構造を優先する
- タイマーとログ生成タイマーはゲーム終了時に必ず解除する
- 一時停止中はタイマーとログアニメーションを停止する
- 操作不能になる長い演出を追加しない
- 色だけで正誤やログレベルを区別しない

## 素材ルール

- 素材追加前に `docs/assets.md` を確認する
- 仮素材を本番素材として扱わない
- 画像へ日本語タイトルや説明を直接描画しない
- キャラクター差分の外見を統一する
- 音声なしでもゲーム進行可能にする
- 音声追加時はミュートと音量設定を実装する

## 仕様変更時の更新先

| 変更内容 | 更新先 |
|---|---|
| ゲーム概要・公開URL | `README.md` |
| ルール・条件・スコア | `docs/game-design.md` |
| ログ生成・判定・進行 | `docs/game-loop.md` |
| レイアウト・操作・演出 | `docs/ui-design.md` |
| 画像・音声・仮素材 | `docs/assets.md` |
| 開発ルール | `AGENTS.md` |

## 判断基準

1. 説明なしで最初の操作を理解できるか
2. スマートフォン片手操作で成立するか
3. ログ本文を瞬時に読めるか
4. 正解と失敗の理由が分かるか
5. アニメーションが操作結果を補強しているか
6. 1プレイを短時間でリトライできるか
7. GitHub Pagesのみで公開できるか

単に演出や文字量を増やし、検索判断と操作感を弱める変更は追加しないでください。