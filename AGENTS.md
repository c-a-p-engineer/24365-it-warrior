# AGENTS.md

このリポジトリで作業するAIエージェントおよび開発者向けの基本ルールです。

## 作業前に読む資料

以下の順番で確認してください。

1. `README.md`
   - プロジェクト概要、公開URL、現在の実装範囲を確認する
2. `docs/game-design.md`
   - キャラクター、能力、会社、カード、障害、会社業務の仕様を確認する
3. `docs/game-loop.md`
   - 1日4ターン、情報開示、持ち越し、週次進行を確認する
4. `docs/assets.md`
   - 素材の状態、本番サイズ、色、透過、音の要件を確認する
5. `index.html` / `styles.css` / `app.js`
   - 現在のMVP実装を確認する

仕様を追加または変更するときは、実装だけでなく該当資料も同時に更新してください。

## 固定方針

- ゲーム名は **24365 IT戦士**
- GitHub Pagesで公開する
- ネットワーク対戦は実装しない
- 1人用ブラウザゲームとする
- 1ステージを1日として扱う
- 1日は最大4ターン：出勤時、ランチ、定時後、深夜対応
- ハイスコアは連勤日数とし、結果を職務経歴書風に表示する
- 障害は複数同時・複数属性を許可する
- 原因・属性は職種能力、調査能力、調査カードにより開示する
- 原因未特定でも解決可能だが、最適対応は表示しない
- 誤対応は障害の危険度を上げる
- 持ち越し不可障害を残すとゲームオーバー
- 障害がない時間は会社業務または退勤を選べる
- 曜日ごとに障害傾向を変え、金曜日は強化する
- 会社ごとにゲーム傾向を変える
- 7日ごとに残留・転職・昇進・支援交代の機会を設ける
- 支援キャラクターは職位に応じて最大2名
- PCとスマートフォンの両方を考慮する
- 静的ホスティングで成立する構成にする

## 実装上の注意

- GitHub Pagesのサブパス `/24365-it-warrior/` で動作する相対パスを使用する
- サーバーサイド処理を前提にしない
- セーブデータはLocalStorageまたはIndexedDBを使用する
- 操作はマウスとタッチの両方に対応する
- 大容量の画像、音声、動画素材を避ける
- ゲームロジックと表示処理を可能な範囲で分離する
- カード、障害、職種、会社の定義はデータ駆動で追加できる構成を優先する
- 色だけで状態を伝えず、危険度や属性を文字でも表示する
- 320px幅で横スクロールを発生させない

## 素材ルール

- 素材追加前に `docs/assets.md` の仕様を確認する
- 仮素材を本番素材として扱わない
- 素材追加・差し替え時はステータスを更新する
- 透過指定素材へ背景色を焼き込まない
- 生成画像内へ日本語タイトルや説明文を直接描画しない
- 同一キャラクターの髪型、顔、衣装、配色を維持する
- 音声は無音でもゲーム進行可能にする

## 仕様変更時の更新先

| 変更内容 | 主な更新先 |
|---|---|
| ゲーム全体・公開URL | `README.md` |
| 職種・支援・会社・カード・障害・会社業務 | `docs/game-design.md` |
| 曜日・戦闘・ターン・週次進行 | `docs/game-loop.md` |
| 画像・音声・UI素材 | `docs/assets.md` |
| 開発ルール・参照順 | `AGENTS.md` |
| Pages公開 | `.github/workflows/deploy-pages.yml` |

## 判断に迷った場合

1. 1人で遊んで成立するか
2. GitHub Pagesのみで公開できるか
3. 連勤を続けるカードゲームの核を強化するか
4. IT業界のネタがゲーム上の判断として機能するか
5. スマートフォンでも操作可能か
6. 既存の仮素材・本番素材の区別を維持できるか

単に要素を増やすだけで、判断、周回性、職種差、会社差を強化しない機能は追加しないでください。
