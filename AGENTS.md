# AGENTS.md

このリポジトリで作業するAIエージェントおよび開発者向けのルールです。

## 作業前に読む資料

1. `README.md`
   - 公開URL、現在のゲーム概要、実装範囲
2. `docs/game-design.md`
   - バグレベル、勤務先、パッチ、得点、ゲームオーバー条件
3. `docs/game-loop.md`
   - スワイプ、マージ、安定度、連勤の処理順
4. `docs/ui-design.md`
   - スマートフォン優先の画面・操作・演出方針
5. `docs/assets.md`
   - 仮素材、本番素材、画像・音声仕様
6. `index.html` / `styles.css` / `app.js`
   - 現在の実装

仕様変更時は、実装だけでなく該当資料も同時に更新してください。

## 固定方針

- 正式名は **24365 IT戦士**
- 副題は **バグマージ**
- GitHub Pagesで公開する
- 1人用ブラウザゲームとする
- ネットワーク通信・対戦は実装しない
- スマートフォン縦画面を最優先する
- 1プレイ3〜5分を目標にする
- 盤面は4×4を基本とする
- 同レベルのバグをスワイプでマージする
- マージでパッチを獲得し、任意のバグを除去できる
- 緊急・本番障害は本番安定度を低下させる
- 10操作を1日とし、連勤日数を記録する
- 5日ごとの金曜日は負荷を増やす
- 勤務先ごとに初期値とプレイ傾向を変える
- 得点を主ハイスコア、連勤日数を副記録とする
- LocalStorageへ最高記録を保存する

## 実装上の注意

- GitHub Pagesのサブパスで動く相対パスを使用する
- サーバーサイド処理を前提にしない
- 外部APIをゲーム進行に必須としない
- タッチ、マウス、キーボードで操作できること
- スワイプ不能環境向けに方向ボタンを残す
- 320px幅で横スクロールを発生させない
- 主要プレイ画面は可能な限り1画面内へ収める
- 色だけでバグレベルを区別しない
- `prefers-reduced-motion`を尊重する
- 大容量の画像・音声・動画を避ける
- 初回ロードは可能な限り5MB以下を維持する

## ゲームロジックの原則

- 盤面が変化しない入力では、新しいバグを発生させない
- 1操作中に生成されたタイルを同じ操作で再マージしない
- 本番障害同士のマージはレベル5で上限とする
- パッチ費用と得点は `LEVELS` 定義を単一の情報源とする
- 勤務先差は `COMPANIES` 定義へ集約する
- 調整用数値をHTMLやCSSへ分散させない
- ゲームオーバー判定は安定度と有効手の両方を確認する

## 素材ルール

- 素材追加前に `docs/assets.md` を確認する
- 絵文字・CSSは仮素材として扱う
- 本番素材追加時は台帳のステータスを更新する
- 生成画像内へ日本語タイトルや説明文を描画しない
- 透過素材へ背景色を焼き込まない
- 無音でもゲームを完走できること
- 音声追加時はミュート設定を実装する

## 仕様変更時の更新先

| 変更内容 | 更新先 |
|---|---|
| ゲーム全体・公開URL | `README.md` |
| バグ、勤務先、パッチ、得点 | `docs/game-design.md` |
| マージ、連勤、安定度 | `docs/game-loop.md` |
| 画面、操作、演出 | `docs/ui-design.md` |
| 画像・音声素材 | `docs/assets.md` |
| 開発ルール | `AGENTS.md` |

## 判断基準

1. 3秒以内に操作方法を推測できるか
2. 1回の入力で盤面が明確に変化するか
3. スマートフォンで片手操作できるか
4. リトライしたくなる短さか
5. IT題材が単なる名称ではなく、リスク判断として機能するか
6. GitHub Pagesだけで成立するか

既存要素を増やすより、マージ・修正・安定度の3要素を強化してください。
